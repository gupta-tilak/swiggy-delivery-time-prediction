from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Building a ML project to predict delivery time for food delivery apps in minutes.',
    author='Tilak',
    license='',
)
